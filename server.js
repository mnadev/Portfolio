const http = require('http');
const fs = require('fs');

const hostname = '127.0.0.1';
const httpsPort = 443;
const httpPort = 80;

http.createServer(

).listen(80)

http.createServer(

).listen(443)
